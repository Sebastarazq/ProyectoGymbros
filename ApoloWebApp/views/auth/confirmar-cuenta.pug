extends ../layout/index

block contenido
    .container
        .py-10
            h1.text-4xl.my-10.font-extrabold.text-center.text-white Apolo
            h2.text-center.text-2xl.text-white= pagina

        div
            p(class= error ? 'bg-red-500' : 'bg-green-500').py-2.px-5.rounded-lg.w-full.font-bold.text-center.text-white.uppercase=mensaje

            if !error
                a(href="/auth/login" class="text-center font-bold text-sm text-white uppercase mt-10 block") Ya puedes inciar Sesion